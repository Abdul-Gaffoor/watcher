{
  "name": "watcher-api",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Auth + CloudFront signed-cookie issuer. Intentionally dependency-free.",
  "main": "src/index.mjs",
  "engines": { "node": ">=20" },
  "scripts": {
    "test": "node --test \"test/**/*.test.mjs\""
  }
}
